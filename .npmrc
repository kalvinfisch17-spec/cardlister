auto-install-peers=false
strict-peer-dependencies=false

# Include platform-specific binaries for all supported platforms
# so the lockfile works on both Linux (Replit) and Windows (local dev)
supported-architectures-os=current,linux,win32
supported-architectures-cpu=current,x64,arm64
